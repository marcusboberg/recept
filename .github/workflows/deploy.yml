name: Legacy GitHub Pages Deploy (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "GitHub Pages deploy is disabled."
          echo "This project now relies on SSR routes and should be deployed on Vercel."
